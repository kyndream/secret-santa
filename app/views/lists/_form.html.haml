= form_for(list) do |f|
  - if list.errors.any?
    #error_explanation
      %h2
        = pluralize(list.errors.count, "error")
        prohibited this list from being saved:
      %ul
        - list.errors.full_messages.each do |message|
          %li= message
  = f.text_field :name, placeholder: 'Name*'
  = f.text_field :description, placeholder: 'Description (optional)'
  = f.text_field :price_cap, placeholder: 'Price cap (optional)'
  = f.submit "Create List", class: 'btn submit'
